export component VersionsPage {
    VerticalLayout {
        alignment: start;
        padding: 32px;
        spacing: 8px;

        Text {
            text: "OptiScaler Versions";
            color: #e2e2e5;
            font-size: 28px;
            font-weight: 700;
        }

        Text {
            text: "Manage downloaded OptiScaler releases.";
            color: #555c66;
            font-size: 14px;
        }
    }
}
