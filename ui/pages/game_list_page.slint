export component GameListPage {
    VerticalLayout {
        alignment: start;
        padding: 32px;
        spacing: 8px;

        Text {
            text: "Game Library";
            color: #e2e2e5;
            font-size: 28px;
            font-weight: 700;
        }

        Text {
            text: "Scan your system to find installed games.";
            color: #555c66;
            font-size: 14px;
        }
    }
}
