export component SettingsPage {
    VerticalLayout {
        alignment: start;
        padding: 32px;
        spacing: 8px;

        Text {
            text: "Settings";
            color: #e2e2e5;
            font-size: 28px;
            font-weight: 700;
        }

        Text {
            text: "Configure OptiTux preferences.";
            color: #555c66;
            font-size: 14px;
        }
    }
}
