export component AboutPage {
    VerticalLayout {
        alignment: start;
        padding: 32px;
        spacing: 8px;

        Text {
            text: "About OptiTux";
            color: #e2e2e5;
            font-size: 28px;
            font-weight: 700;
        }

        Text {
            text: "An open-source OptiScaler frontend for Linux.";
            color: #555c66;
            font-size: 14px;
        }
    }
}
