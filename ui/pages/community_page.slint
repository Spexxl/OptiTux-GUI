export component CommunityPage {
    VerticalLayout {
        alignment: start;
        padding: 32px;
        spacing: 8px;

        Text {
            text: "Community";
            color: #e2e2e5;
            font-size: 28px;
            font-weight: 700;
        }

        Text {
            text: "Connect with other OptiTux users.";
            color: #555c66;
            font-size: 14px;
        }
    }
}
